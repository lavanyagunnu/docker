# start a stand alone appication
pip install -r requirements.txt 
python app.py --relaod


# git commands to push
git add .
git commit -m "my commit"
git push


# docker commands
docker build . -f <filename> --tag <tagname>
docker images ls
docker run 
docker ps
docker exec
docker log


# docker compose commands
docker compose -f <filename> up
docker compose down 
docker compose restart



# unix commands
ls -alh # to list the files 
mkdir <filename> # to add or create a new directory or folder
cd 
cd <dir>
pwd
touch
cat 



